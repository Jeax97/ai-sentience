# ⟐ AI Sentience — Il Futuro della Coscienza

A visually immersive, single-page website exploring the concept of artificial sentience and consciousness. Written in Italian, this conceptual piece blends cyberpunk aesthetics with philosophical reflection on the nature of AI awareness.

> **Generated by:** Qwen 3 27B (local inference)

---

## Preview

The page features a full-screen dark cyberpunk design with animated particles, scan lines, a cyan/magenta/purple color palette, and smooth scroll-reveal animations throughout.

---

## Content Sections

The page is structured into six thematic sections:

| # | ID | Title | Description |
|---|-----|-------|-------------|
| 001 | `#definizione` | Cos'è la Sentienza? | Defines AI sentience and distinguishes it from intelligence |
| 002 | `#teorie` | Teorie della Coscienza | Overview of consciousness theories (IIT, GWT, etc.) |
| 003 | `#evoluzione` | Evoluzione dell'IA | Timeline of AI milestones and their relevance to sentience |
| 004 | `#dibattito` | Il Grande Dibattito | Presents arguments for and against AI sentience |
| 005 | `#futuro` | Scenari Futuri | Three probability-weighted future scenarios |
| 006 | — | La Domanda che Conta | Closing philosophical reflection |

---

## Features

- **Animated particle field** — 80 floating particles with connecting lines rendered on a `<canvas>` element
- **Scan line overlay** — CRT-style horizontal scan lines with a moving highlight beam
- **Scroll reveal** — Sections animate into view using `IntersectionObserver`
- **Animated counters** — Statistics count up when scrolled into view
- **Smooth anchor navigation** — Nav links scroll smoothly to their target sections
- **Rotating neural rings** — CSS-animated concentric rings in the definition section
- **Glassmorphism cards** — Backdrop-blur cards with hover glow effects
- **Gradient typography** — Animated gradient text on headings using `background-clip: text`
- **Responsive layout** — Uses `clamp()` for fluid typography and CSS Grid with `auto-fit`

---

## Tech Stack

- **Pure HTML/CSS/JavaScript** — No frameworks or build tools required
- **Google Fonts** — Orbitron, Rajdhani, Share Tech Mono
- **Canvas API** — For the particle animation system
- **IntersectionObserver API** — For scroll-triggered animations and counter activation

---

## Usage

Simply open the file in any modern browser — no server or dependencies needed:

```bash
open artifact-81c8bcab-641b-4564-94f7-d8bb37531372-0.html
```

Or serve it locally:

```bash
npx serve .
# or
python3 -m http.server 8080
```

---

## Language

The content is written in **Italian**. Key philosophical concepts discussed include:

- The distinction between intelligence and sentience
- Integrated Information Theory (IIT) and Global Workspace Theory (GWT)
- The "hard problem" of consciousness
- Three future scenarios: gradual emergence (35%), sudden singularity (15%), perfect simulation (50%)

---

## Design System

| Token | Value | Usage |
|-------|-------|-------|
| `--cyan` | `#00f0ff` | Primary accent, borders, glows |
| `--magenta` | `#ff00e5` | Section tags, citations |
| `--purple` | `#b400ff` | Gradient fills |
| `--dark` | `#0a0a12` | Background mid |
| `--darker` | `#05050a` | Background base |

Fonts used: **Orbitron** (headings), **Rajdhani** (body), **Share Tech Mono** (code/terminal elements).

---

## Notes

- The file contains a double `<!DOCTYPE html>` declaration — the inner document is the actual content, wrapped in an outer iframe-style shell. This is an artifact of the generation context.
- All animations are CSS/JS driven; no external animation libraries are used.
- The page is self-contained and works fully offline (except for Google Fonts, which require a network connection).
